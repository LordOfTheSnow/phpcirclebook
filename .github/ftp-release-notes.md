This release includes a ready-to-upload `phpcirclebook-ftp-*.zip` asset with all
Composer dependencies (`vendor/`) already installed, for FTP-only shared hosting
without shell access. See the "Installing via FTP (no shell access)" section of the
README for upload instructions.

See CHANGELOG.md for the full list of changes in this version.
